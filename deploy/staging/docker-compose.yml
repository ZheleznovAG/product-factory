services:
  catalog-service:
    image: ${CATALOG_SERVICE_IMAGE:-ghcr.io/example/catalog-service:staging}
    restart: unless-stopped
    environment:
      PORT: 8080
    ports:
      - "${CATALOG_SERVICE_PORT:-18080}:8080"
