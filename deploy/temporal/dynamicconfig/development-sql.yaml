# Minimal dynamic config for Temporal Server with PostgreSQL (from temporalio/samples-server).
limit.maxIDLength:
  - value: 255
    constraints: {}
system.forceSearchAttributesCacheRefreshOnRead:
  - value: true
    constraints: {}
