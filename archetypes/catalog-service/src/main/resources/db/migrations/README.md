# DB migrations

Place Flyway SQL migrations here, for example:

- `V1__init_catalog.sql`
- `V2__add_category_index.sql`

This archetype keeps the folder ready so migration tooling can be added without changing the project layout.
