# JDK 17 для сборки (проект требует jvmToolchain(17)).
# Не добавлять org.gradle.java.home сюда — иначе падает сборка на GitHub (путь macOS).
# Локально: задай JAVA_HOME в оболочке или в ~/.gradle/gradle.properties:
#   org.gradle.java.home=/opt/homebrew/opt/openjdk@17
